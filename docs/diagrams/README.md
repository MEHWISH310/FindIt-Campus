# Diagrams

Architecture and flow diagrams for the report/paper go here:

- `architecture.png` — overall system diagram (frontend/backend/DB/embedding models)
- `matching_pipeline.png` — text/image embedding → fusion → calibration → disambiguation flow
- `verification_flow.png` — asymmetric verification flow (lost vs found reports)
- `er_diagram.png` — database entity-relationship diagram (Report, Match, CustodyRecord)

Export from draw.io / Excalidraw / Figma as PNG or SVG.